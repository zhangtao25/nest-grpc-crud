import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { Document } from 'mongoose';

@Schema()
export class <%- column.Name %>Document extends Document {
    @Prop()
    id: number;

    @Prop()
    activityId: number;

    @Prop()
    customs: any;
}

export const <%- column.Name %>Schema = SchemaFactory.createForClass(<%- column.Name %>Document);